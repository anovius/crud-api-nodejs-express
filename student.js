class Student{
    constructor(name, rollNo, batch, password ){
        this.name = name
        this.rollNo = rollNo
        this.batch = batch
        this.password = password
    }
}

module.exports = Student;